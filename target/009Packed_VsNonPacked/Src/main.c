/**
  ******************************************************************************
  * @file    main.c
  * @author  Auto-generated by STM32CubeIDE
  * @version V1.0
  * @brief   Default main function.
  ******************************************************************************
*/

#include <stdio.h>

typedef struct{
	char  data1;
	int   data2;
	char  data3;
	short data4;
}DataSet_t;


DataSet_t data; //this consumes 12 bytes in memory (RAM)


int main(void)
{
	data.data1 = 0xAA;
	data.data2 = 0xFFFFEEEE;
	data.data3 = 0x55;
	data.data4 = 0xA5A5;

	printf("data.data1 = %d\n", data.data1);
	printf("data.data2 = %d\n", data.data2);
	printf("data.data3 = %d\n", data.data3);
	printf("data.data4 = %d\n", data.data4);



	/* 1. A structure type cannot contain itself as a member
	 *
	 * struct CarModel{
		unsigned int carNumber;
		uint32_t     carPrice;
		uint16_t     carMaxSpeed;
		float		 carWeight;
		struct CarModel carBMW;//this is not allowed (it will throw compilation error)
	}; */


	/* 	2. Structure types can contain pointers to their own type
	 * Such self-referential structures are used
	 * in implementing linked list and binary trees
	 *
	 * struct CarModel{
		unsigned int carNumber;
		uint32_t     carPrice;
		uint16_t     carMaxSpeed;
		float		 carWeight;
		struct CarModel *pcarBMW;//this is  allowed
	}; */


	/* 3. Nested structure is nothing but structure inside a structure*/

	/*struct Data{
		char   data1;
		int    data2;
		char   data3;
		short  data4;
		struct{
			char  data5;
			int   data6;
		}moreData;//moreData is neither a tag name nor typedef name, This is just variable
	};*/

	/*struct CarModel{
		unsigned int carNumber;
		uint32_t     carPrise;
		uint16_t     carMaxSpeed;
		float		 carWeight;
		struct{
			float temperature;
			float airPressure;
			int   fuel;
		}carParameters;
	};*/



	for(;;);
}
